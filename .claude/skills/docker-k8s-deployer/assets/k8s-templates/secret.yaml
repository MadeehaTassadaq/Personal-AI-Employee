apiVersion: v1
kind: Secret
metadata:
  name: APP_NAME-secrets
  namespace: NAMESPACE
  labels:
    app: APP_NAME
type: Opaque
stringData:
  DATABASE_URL: "postgresql://user:password@db:5432/app"
  SECRET_KEY: "change-me-in-production"
  # Add sensitive configuration here
  # stringData values are base64 encoded automatically
